# Fairy-DSH 整理版 · 交接摘要

> 用途：把这份贴进新对话，接手的 AI 立刻具备全部上下文。
> 生成时间：**2026-09-13**（首版写于 v0.2.3 发布后；同日已推进到 **v0.3.0 发布完成**）。作者：孤舟蓑笠。

> 📌 **最新一轮的增量摘要**：[`接手-v0.3.2.md`](接手-v0.3.2.md)（第三轮：第二个朗读引擎 MOSS-TTS-Nano）。
> **本文是全量档案，那份是这一轮的增量；两者一起看，冲突时以那份为准。**

---

## ⚠️ 最重要的一条前提：上游的终极目标是独立的 Electron 桌宠应用

**橙汁本色明确表示：他的最终目标是把 Fairy 做成一个独立的 Electron 应用**
（不依赖 DSH 启动，走桌宠互动路线），而不是长期做一个 DSH 插件套件。

**这一条解释了本仓库能看到的所有"上游问题"。它们不是缺陷，是定位差异：**

| 现象 | 真实原因 |
| --- | --- |
| `runtime/`（`index.js` / `fairy_core.py` / `safety-gate.js`）被 `.gitignore` 排除 | 那是**他 app 的核心**，不是"忘了发布" |
| 5 个包 `"private": true`、依赖写 `link:../../fairy-contracts` | 它们是**他 app 的组件**，用的是开发期写法 |
| 没有 `dsh.bundle.patch` / `cordis.patch.yml` | 他从没打算让外人 `dsh plugin add` |
| `README.md` 自述"开源发布候选目录""不含 node_modules"、只给 `.sh` 脚本 | 这是**开发仓库**，不是分发渠道 |
| `profiles/web/`、`fairy-system/` 一堆验收工具 | 他自己的开发脚手架 |

**⚠️ 因此**：`agent.cordis.yml` 里 `fairy-core-runtime` / `fairy-safety-gate` 两条引用
**不是上游的 bug**，不要以"修 bug"的姿态去提；它是有意为之（详见 `docs\分发策略.md`）。

---

## ✅ 当前状态：**已获橙汁授权，可以重构仓库**

橙汁本色原话（2026-09-13）：

> 「你想咋搞都可以 / 但我的想法是要更新就做完善之后再做大更新」

**两条附加约束：**

| 约束 | 内容 |
| --- | --- |
| **红线** | 他说的"核心文件"= **布局 / 人设 / 风格 / 界面绘画之类的外观表现**。**表现层一个字节都不许改** |
| **节奏** | 不要零敲碎打。**攒成一个完善的大版本，一次性发**（目标 v0.3.0） |

### 0.1 红线清单（不许碰）

- `.agent-presets/fairy/` 下**所有语料内容**（canon / personality / style / behavior / preset.yml）—— **只可搬位置，不可改内容**
- `fairy-visual/src/client/**`（含 149KB 的 `style.js`）：布局、mascot、composer 坞、滚动条、视觉过渡
- `fairy-voice` 的朗读逻辑
- `LICENSE` / `NOTICE` / `THIRD_PARTY_NOTICES.md` / `TRADEMARKS.md`

> 自己新增的**设置栏合并面板**、**朗读自检面板**属于"新增"，保留。

### 0.2 🔑 关键突破：installer 可以废掉

**`dsh plugin add` 官方命令就能一步到位**（自动装依赖 + 自动注册 bundle），
群友只需 1 条命令，**不再需要 `install.ps1` / `uninstall.ps1`**。

完整实测记录见 **`docs\安装机制实测.md`**，核心结论：

| 形态 | 装依赖 |
| --- | --- |
| `link:<目录>` | ❌ **不装** —— 所以现在才必须预装 `node_modules` |
| `file:<目录>` / `<xxx.tgz>` | ✅ 装 |

→ **目标架构**：分发 5 个 `.tgz`（单个约 374KB，对比现在 1.8MB 的 zip），群友一行命令装完。

---

## 1. 项目是什么

| 项 | 值 |
| --- | --- |
| 项目名 | **Fairy-DSH 整理版**（可安装整合包） |
| 一句话 | 把上游 Fairy-DSH 这套 DSH 插件整理成"开箱即装"的分发包：安装器 + 卸载器 + 隔离验证 + 打包脚本 + 设置栏合并 + 朗读自检 + 人设一键默认 |
| 面向上游 | 使用者是 QQ 群友（交流群 **1124349108**），多为新手 |
| 当前版本 | **0.3.2**（仓库根 **`VERSION`** 文件，已发布）—— 0.3.1 / 0.3.0 / 0.2.3 是更早的已发布版本 |
| 许可 | 上游原创代码 Apache-2.0；本分支为非官方整理分支 |

## 2. 仓库三角关系

| 角色 | 地址 |
| --- | --- |
| 上游（作者：橙汁本色） | https://github.com/Chengzhibense/Fairy-DSH （`main @ d639887`） |
| 我的 fork（**纯净镜像**，不改内容） | https://github.com/Guzhou2002/Fairy-DSH |
| 我的主仓库（开发 + 发布） | https://github.com/Guzhou2002/Fairy-DSH-Optimized |
| 发布页（Release **v0.3.2**，当前） | https://github.com/Guzhou2002/Fairy-DSH-Optimized/releases/tag/v0.3.2 |
| 旧发布页（v0.3.1，已被取代） | https://github.com/Guzhou2002/Fairy-DSH-Optimized/releases/tag/v0.3.1 |
| 旧发布页（v0.3.0，已被取代） | https://github.com/Guzhou2002/Fairy-DSH-Optimized/releases/tag/v0.3.0 |
| 旧发布页（v0.2.3，已被取代） | https://github.com/Guzhou2002/Fairy-DSH-Optimized/releases/tag/v0.2.3 |
| 维护 fork 的手册 | `docs\仓库与上游.md` |

## 3. 本地路径与 git 状态（原样）

**本地路径**：`C:\Users\1\.dsh\plugins\Fairy-DSH`

**`git remote -v`**：

```
origin	https://github.com/Guzhou2002/Fairy-DSH-Optimized.git (fetch)
origin	https://github.com/Guzhou2002/Fairy-DSH-Optimized.git (push)
upstream	https://github.com/Chengzhibense/Fairy-DSH.git (fetch)
upstream	https://github.com/Chengzhibense/Fairy-DSH.git (push)
```

**`git branch -vv`**：

```
* main 95d0c28 [origin/main] docs: 明确非官方分支身份，问题请反馈到本仓库/群而不是上游作者
```

**当前分支**：`main`（跟踪 `origin/main`，HEAD = `95d0c28`，工作区干净）
**标签**：`v0.2.3`、`v0.3.0`

> 📌 **2026-09-13 之后的实际状态（本节上方是当天的快照，已过时）**
> - HEAD 已到 **`1fb2ed2`**（v0.3.1 合并提交），工作区干净，与 `origin/main` 同步
> - tag **`v0.3.1`** 已指向 `1fb2ed2`；`v0.3.0` / `v0.2.3` 保留
> - remote **增加 `myfork`** → `https://github.com/Guzhou2002/Fairy-DSH.git`（备份仓库 / 纯净上游镜像）
> - `upstream` 的**推送地址已设为 `DISABLED`**（防误推橙汁仓库，拉取不受影响）
> - 新增 git worktree `upstream-pull-request\`，分支 `pr/cordis-bundle`（基点上游 `d639887`，`5d7256f`）
> - 最新的 remote 说明以 `docs\仓库与上游.md` §2 为准
**提交身份（仓库级，不动全局配置）**：`孤舟蓑笠 <578778930@qq.com>`

**`git log --oneline -6`**：

```
95d0c28 docs: 明确非官方分支身份，问题请反馈到本仓库/群而不是上游作者
a644e37 docs: 补上仓库地址、fork 血缘说明与 Release 附件命名坑
dcd9deb 整理版 v0.2.3：安装器、设置栏合并、朗读自检、人设一键默认
d639887 Fix GitHub Actions pnpm cache paths          ← 上游
4c1a161 Initial public Fairy DSH plugin suite        ← 上游
```

**与上游差异规模**：`112 files changed, 15232 insertions(+), 4853 deletions(-)`

**被 git 忽略**（不进仓库）：六个 `node_modules/`、`release/`、`snapshots/`

## 4. 我做的修改

### 4.1 分发与安装（本分支核心价值）

- `install.ps1` + `install.cmd`：数字键菜单选插件（1 视觉 / 2 +语音 / 3 +余额 / 4 全部 5 个 / 5 自定义），7 步流程含依赖检查、设置栏合并、人设预设安装、配置备份、`dsh plugin add` 注册、遗留块清理、profile 校验
- `uninstall.ps1` + `uninstall.cmd`：5 步，只移除实际存在的依赖
- `verify-isolated.ps1` + `verify.cmd`：临时 `DSH_HOME` 起隔离实例 + 无头浏览器探针，**不碰使用者现有 profile**
- `build-release.ps1`：产出两个 zip（一键安装版 / 一键部署版-含依赖）+ `.sha256`，默认输出到包内 `release\`
- `安装说明.txt`：傻瓜版图文步骤（含第八步人设、第九步朗读自检 9.1–9.4）

### 4.2 部署机制（0.2.1 起）

每个插件包的 `package.json` 声明 `dsh.bundle.patch` + 自带 `cordis.patch.yml`，于是 `dsh plugin add` **一条命令**即可完成依赖链接 + `dsh.profile.bundles` 注册。

### 4.3 可独立分发（0.2.0–0.2.1）

- `dsh-fairy-contracts` 依赖**内联**进各包 `vendor/`（源码级复制），依赖里移除
- 依赖布局改 **hoisted 扁平**（各包 `pnpm-workspace.yaml: nodeLinker: hoisted`），`node_modules` 内**零符号链接**，解压即用

### 4.4 设置栏合并

`lib/settings-merge.ps1` 从 `upstream-originals\` 的原件**生成**两个客户端的合并包：

- `fairy-visual\…\lib\client.js` ← 注入 `FairyNotice`（顶部声明行）+ `FairyVoicePanel`（自检面板 / 配置 / 人设开关），标签统一为 **Fairy**
- `fairy-voice\…\lib\client.js` ← 去掉它自己的 `settings.section` + 注入诊断通道 `globalThis.__FAIRY_VOICE_DIAG__`
- 命令：`. .\lib\settings-merge.ps1; Set-SettingsMerge -Root (Get-Location) -Mode merged -Snapshot -Force`

### 4.5 0.2.3 新功能（本版重点）

- **朗读功能自检面板**：7 项检查（插件宿主 / SoVITS 可达 / 参考音频 / **真实合成一句话** / 浏览器音频能力 / 朗读控件挂载 / 消息识别），逐项给「怎么修」，一键复制诊断信息（不含聊天内容）
- **SoVITS 地址 + 参考音频路径可配置**：默认 `http://127.0.0.1:9880`，可改端口或另一台机器；保存后**即时生效**；非法输入给人话拒绝；配置存 `~/.dsh/fairy-voice/runtime/config.json`
- **Fairy 人设一键设为默认预设**：写 DSH 的 `agent-presets.default`，写前备份原值（`$DSH_HOME\.fairy-persona\default-preset-backup.json`），可还原；无备份时回落到部署默认（不写空串）
- **首次使用醒目提示**：设置页顶部红色提示框（列出还差什么 + 按钮）；启动时 `webServer.tapIndex()` 注入脚本 → 设置图标红点 + 右下角红色提示框（可"不再提示"）
- **HDD 视觉模式前置检查**：视觉模式不开 → 朗读控件压根不出现（上游设计）

### 4.6 文档

`README.md`、`RELEASE-NOTES.md`、`安装说明.txt`、`docs\仓库与上游.md`、`docs\验证报告.md`

> 上游文件的所有改动都用 `[local patch 0.2.x]` 注释标注（Apache-2.0 §4(b) 要求），并在 README「本地改动」章节逐条说明。

## 5. 验证情况（实测）

已验证：

- 设置面板渲染（无头 Chrome 探针 `tools\settings-probe.mjs`）：小节顺序 **人设预设 → 朗读功能自检 → 朗读服务设置 → 语音简报**；7 项检查、状态条、两个可配置输入框、复制按钮全部落地；`exceptions: []`
  > 📌 **0.3.2 起顺序有变**：改为 **人设预设 → 朗读设置 → 朗读自检 → 语音简报**（先配置后验证），
  > 且自检明细默认折叠、结论行常显。原顺序的记录保留在上一行，不删。
- 一键默认预设：`off → on`（宿主 `isDefault:true, defaultPreset:fairy`）→ 取消 → 还原 `standard`，并核对 `settings.yaml` 落盘
- 无备份时取消默认 → 回落 `standard`（旧实现会写空串，已修）
- 视觉模式关闭时该项 ❌ 并给出修法（对照实测）
- 配置链路：默认值 → 保存 `http://127.0.0.1:9999` + 自定义参考音频 → 再读为新值 → 自检按新地址报错 → 非法地址 / 相对路径被拒
- 端到端：解压 zip → `dsh plugin --profile … add link:…` 一条命令 → 宿主 `2/2 apply success` → `/fairy-persona/status` 200 → toggle 落地 12 个人设文件 → 关闭后移除干净
- Release 附件与本地包字节数一致（`1867098` / `789691`）

**尚未验证**：**无**（下面两条已于 v0.3.1 完成）

1. ✅ **「消息识别」那一项**：2026-09-13 在 live 实例实测 —— 修复适配层后自检第 7 项 ✅
   「当前会话有 20 条可朗读的回复」，会话页朗读按钮也正常出现
2. ✅ **Release 附件下载回来复算 SHA**：v0.3.1 发布后，把 **7 个附件全部下载、逐字节复算 SHA256，全部一致**
   —— 一并作废"本机下载 GitHub 大文件会卡死"的旧结论（真正原因是 git 没走代理，见 §9 坑 12 / 坑 15）

## 6. 目标

- **短期**：群友装上就能用；语音朗读出问题时能自己诊断出卡在哪一层
- **中期**：语音引擎可插拔 —— `fairy`（GPT-SoVITS）+ 浏览器原生 `speechSynthesis` 兜底 + 自动降级（用户已认可方向，未动工）
- **长期可选**：SoVITS「一键盘」独立包（自动检测显卡/显存/磁盘/Python，装 `api_v2.py` + 中性参考音），**不随插件包分发**

明确**不做**的把 SoVITS 推理端整合进插件包（体积 6–9 GB、需要 NVIDIA 显卡、权重与参考音不适合二次分发）。

## 7. 待办

| # | 事项 | 谁做 |
| --- | --- | --- |
| 1 | ✅ **已完成**（2026-09-13）：设置 → Fairy 打开「**启用**」，朗读控件随之出现 | — |
| 2 | ✅ **已完成**（2026-09-13）：真实会话自检，第 7 项「消息识别」已是 ✅（20 条可朗读回复） | — |
| 3 | 可选：语音兜底引擎（浏览器 TTS + 自动降级） | 待定 |
| 4 | 可选：SoVITS 一键盘（独立包） | 待定 |
| 5 | 可选：把 fork 配成本地第三个 remote（`git remote add myfork https://github.com/Guzhou2002/Fairy-DSH.git`），仅在要提 PR 时用 | 待定 |
| 6 | 若已把**旧 SHA** 发到群里，公告需更新为新 SHA（见第 8 节） | **用户** |
| 7 | ✅ **已获橙汁授权**（"你想咋搞都可以，但要做完善再大更新"）—— 见本文开头 §0 | — |
| 8 | **出《重构设计书》**：结构归位 + 打包改 `.tgz` + 删 installer。**设计书审过再动代码** | 待定 |
| 9 | 决定 CI 要不要恢复（`.github\workflows\test.yml` 一恢复，GitHub Actions 就会开始跑） | **用户** |
| 10 | 补测**远程 tarball URL** 形态（见 `docs\安装机制实测.md` §6 遗留问题 4） | 待定 |
| 11 | 🔴 **v0.3.2 Release 正文补三块**（下载哪个 / 装完做什么 / SHA256）—— 现状只有 1215 字符。步骤见 [`接手-v0.3.2.md`](接手-v0.3.2.md) §3 | **用户** |
| 12 | 修 **4 个上游陈旧测试**（`test/index.test.js` 等，从没跟着 v0.2.3 重构更新过）→ 让测试套件重新有绿灯信号 | 待定 |
| 13 | 可选：MOSS 真流式（接它的 4 个 `generate-stream` 端点） | 待定 |

## 8. 产物与校验值（按版本）

> **说明**：v0.3.0 起分发物是 7 个文件（5 个 tgz + `install.cmd` + `install_full.cmd`），
> v0.2.3 及之前是两个 zip（已删除，脚本归档在 `legacy\`）。
>
> - `install.cmd` —— **默认只装 3 个安全插件**（visual / voice / balance）
> - `install_full.cmd` —— 装全部 5 个；**装前会先把 startup / browser-dock 的风险讲清楚并要求确认**
> - ⚠️ 替换 Release 附件时**别忘了把 `install_full.cmd` 一起传上去**，否则 README 里那个链接会 404

`release\` 目录（不进 git），`.\pack.ps1` 产出。**文件名一律不带版本号** ——
因为 README / `install.cmd` 用的是 `.../releases/latest/download/<包名>.tgz` 这种永久地址，
而 `latest/download` 的文件名是**照字面取**的（带版本号则下次发版就 404）。

### v0.3.3（当前，2026-09-14 发布）

> **紧急修包**：v0.3.2 的 `dsh-fairy-voice` **一重启 DSH 就起不来**
> （`ReferenceError: ensureFairyDirectories is not defined`）。
> 起因：v0.3.2 的 `lib/index.js` 调用了该函数却没把它写进 import 清单。
> `node --check` 查不出（语法无误，是运行时 ReferenceError，只有真正启动才暴露）——
> **该提交（`5b91b65`）从未被真正重启验证过**，所以一路发了出去。
> 相对 v0.3.2：`dsh-fairy-voice`（1.0.2 → **1.0.3**）与 `dsh-fairy-visual`（0.1.3 → **0.1.4**）变了，
> 另外 3 个包**字节未变**；两个 `install.cmd` 也**未变**。仓库根 `VERSION` = **0.3.3**。
> 本版内容见 [`RELEASE-NOTES.md`](../RELEASE-NOTES.md) 的「v0.3.3 变更」。

| 文件 | 字节 | SHA256 |
| --- | --- | --- |
| `dsh-fairy-visual.tgz` | 387385 | `11655E73899227D42E385678E743A0DF07B2C2D1ACE4A3A627C9820FAB589191` |
| `dsh-fairy-voice.tgz` | 65043 | `FA2637810BE970BB1D65A202B3959F5170720ADA870B877A5FD43157B9C337EB` |
| `dsh-balance-meter.tgz` | 12791 | `454F6DE02A825A9C5D4CAF75E214660AB727AA6FB389A35B63660697B8A72693` |
| `dsh-fairy-startup.tgz` | 5018 | `EE3C28A3231194CF5670526DA5B85F05BA3B09D4228275D02DD6ED54B8AFF452` |
| `dsh-browser-dock.tgz` | 29592 | `03460446635A5F21DA3E4DBA11D83AF0EFA55564E288BDE1ECF2FCE5C29E4986` |
| `install.cmd` | 6619 | `B35B0FCEF3FF2A5319546C40BB89F6CBCE71D77560BFFAD29A54E6742D4AA55B` |
| `install_full.cmd` | 7470 | `5917A5D6917E38E4CA489A2B8B5A8FB78BFE70416D79F13D5CF275292D518B22` |

**合计 488.1 KB。**

#### v0.3.3 · Release 附件（2026-09-14 发布，**已逐项核对**）

| 项 | 值 |
| --- | --- |
| Release 页 | https://github.com/Guzhou2002/Fairy-DSH-Optimized/releases/tag/v0.3.3 |
| 附件 | 上面那 7 个 |
| ✅ 附件核对 | **7 个逐字节比对全部一致**（`gh api` 取远端 size，与本机逐项比） |
| ✅ 正文 | **2139 字符**，三块齐全（下载哪个 / 装完做什么 / SHA256 7 行）—— 不再重演 v0.3.1/v0.3.2 的漏内容 |
| ✅ 永久地址 | 抽样 3 个 `releases/latest/download/<文件名>` 均 **HTTP 200** 且大小正确；`latest` 已指向 v0.3.3 |
| ✅ tag | `v0.3.3` → 发版提交 **`4e638da`**（`main` 之后另有 `a96abf6` 修验证脚本，**不进任何 tgz，不用挪 tag**） |
| ✅ 隔离验证 | 修好 `verify-isolated.ps1` 后重跑，**RESULT: PASS**，`exceptions: []` |
| ⚠️ v0.3.2 页 | 正文已加置顶警告「此版本有严重缺陷，请不要安装」，附件保留作存档（旧正文备份在 `release\_body-v0.3.2-original.md`） |

> 📌 **发 Release 不用再手工点网页了**：2026-09-14 起本机装了 `gh`，账号 `Guzhou2002` 已登录（token 含 `repo`）：
> ```powershell
> gh release create v0.x.y --title "标题" --notes-file <纯正文文件> --verify-tag <7 个附件>
> ```
> 两个注意点：
> ① **本机直连 GitHub API 可用，不需要配代理**；
> ② 新装的程序**只有新进程才在 PATH 里** —— DSH 进程若早于 gh 安装启动，要用全路径 `C:\Program Files\GitHub CLI\gh.exe`。

> 🔴 **新增发版铁律：推之前必须验包**。
> 解开 `.tgz` 确认 `package/lib/index.js` 里 `ensureFairyDirectories` 命中 **2 处**（import + 调用）：
> ```powershell
> tar -xzf .\release\dsh-fairy-voice.tgz -O package/lib/index.js | Select-String 'ensureFairyDirectories'
> ```
> v0.3.2 就是漏了这道检查，把一个**装完起不来**的包发了出去。
> 更一般地说：**`node --check` 抓不到"调用了一个没导入的符号"，任何改了 `lib/*.js` 的版本都必须真启动一次。**

### v0.3.2（历史，已被取代，2026-09-13 发布）

> 相对 v0.3.1：`dsh-fairy-visual`（0.1.2 → **0.1.3**）与 `dsh-fairy-voice`（1.0.1 → **1.0.2**）变了，
> 另外 3 个包**字节未变**；两个 `install.cmd` 也**未变**。仓库根 `VERSION` = **0.3.2**。
> 本版内容见 [`RELEASE-NOTES.md`](../RELEASE-NOTES.md) 的「v0.3.2 变更」。

| 文件 | 字节 | SHA256 |
| --- | --- | --- |
| `dsh-fairy-visual.tgz` | 387385 | `7000EDC9D8DAD0E6B107F48EF993A8AE613F040276C508BF62FF332ED5F192B4` |
| `dsh-fairy-voice.tgz` | 65056 | `D996D8D730A9BD89283FE7D6D9670790CC625F3118DD8AA7DA6D598270D4ED14` |
| `dsh-balance-meter.tgz` | 12791 | `454F6DE02A825A9C5D4CAF75E214660AB727AA6FB389A35B63660697B8A72693` |
| `dsh-fairy-startup.tgz` | 5018 | `EE3C28A3231194CF5670526DA5B85F05BA3B09D4228275D02DD6ED54B8AFF452` |
| `dsh-browser-dock.tgz` | 29592 | `03460446635A5F21DA3E4DBA11D83AF0EFA55564E288BDE1ECF2FCE5C29E4986` |
| `install.cmd` | 6619 | `B35B0FCEF3FF2A5319546C40BB89F6CBCE71D77560BFFAD29A54E6742D4AA55B` |
| `install_full.cmd` | 7470 | `5917A5D6917E38E4CA489A2B8B5A8FB78BFE70416D79F13D5CF275292D518B22` |

**合计 501.9 KB。** 校验值同时写在 `release\SHA256SUMS.txt`。

> 📌 后 4 个文件（balance / startup / browser-dock / 两个 `.cmd`）与 v0.3.1 **逐字节相同**，
> 但仍要**照传** —— Release 附件是整体替换的。

### v0.3.1（历史，已被取代，2026-09-13 发布）

> 相对 v0.3.0：只有 `dsh-fairy-visual`（0.1.1 → **0.1.2**）和 `dsh-fairy-voice`（1.0.0 → **1.0.1**）变了，
> 另外 5 个文件**字节未变**。仓库根 `VERSION` = **0.3.1**。
> 本版内容见 [`RELEASE-NOTES.md`](../RELEASE-NOTES.md) 的「v0.3.1 变更」。

| 文件 | 字节 | SHA256 |
| --- | --- | --- |
| `dsh-fairy-visual.tgz` | 383687 | `346C3EDBB7D935924FB9ABDC1C54AF8276DF35235392FF5A7E6F75C46C7859D7` |
| `dsh-fairy-voice.tgz` | 55061 | `8D688F89FD9AA9B25D0EC88750A519124F5081B458E7219B40D58C10FC436ABB` |
| `dsh-balance-meter.tgz` | 12791 | `454F6DE02A825A9C5D4CAF75E214660AB727AA6FB389A35B63660697B8A72693` |
| `dsh-fairy-startup.tgz` | 5018 | `EE3C28A3231194CF5670526DA5B85F05BA3B09D4228275D02DD6ED54B8AFF452` |
| `dsh-browser-dock.tgz` | 29592 | `03460446635A5F21DA3E4DBA11D83AF0EFA55564E288BDE1ECF2FCE5C29E4986` |
| `install.cmd` | 6619 | `B35B0FCEF3FF2A5319546C40BB89F6CBCE71D77560BFFAD29A54E6742D4AA55B` |
| `install_full.cmd` | 7470 | `5917A5D6917E38E4CA489A2B8B5A8FB78BFE70416D79F13D5CF275292D518B22` |

**合计 474.8 KB。** 校验值同时写在 `release\SHA256SUMS.txt`（每次 `pack.ps1` 自动生成）。

> 📌 **以上是 v0.3.1 的产物（2026-09-13 发布）**。相对 v0.3.0 只有两个包变了：
> `dsh-fairy-visual` 0.1.1 → **0.1.2**、`dsh-fairy-voice` 1.0.0 → **1.0.1**；
> 另外 5 个文件**字节未变**。仓库根 `VERSION` = **0.3.1**。

#### v0.3.1 · Release 附件

| 项 | 值 |
| --- | --- |
| Release 页（当前） | https://github.com/Guzhou2002/Fairy-DSH-Optimized/releases/tag/v0.3.1 |
| 附件 | 上面那 7 个（**不含** `SHA256SUMS.txt`） |
| 永久地址 | `https://github.com/Guzhou2002/Fairy-DSH-Optimized/releases/latest/download/<文件名>` |
| 已验证 | ✅ **v0.3.1：7 个附件全部下载回来复算 SHA256，与本地逐字节一致**（2026-09-13） |
| tag 对齐 | ✅ `v0.3.1` 已挪到合并提交 `1fb2ed2`（= `main` HEAD），与网页上看到的代码一致 |

> ⚠️ **0.3.0 发布后曾补传过一次 `install.cmd`**（初版漏了 `call dsh`，脚本会在装完插件后静默终止）。
> 当时线上版本哈希与本地一致。
>
> 📌 0.3.0 期间还完成了 **`install.cmd` / `install_full.cmd` 脚本拆分**（默认只装 3 个安全插件）。

### v0.3.0（历史，已被取代）

| 文件 | 字节 | SHA256 |
| --- | --- | --- |
| `install.cmd` | 6619 | `B35B0FCEF3FF2A5319546C40BB89F6CBCE71D77560BFFAD29A54E6742D4AA55B` |
| `install_full.cmd` | 7470 | `5917A5D6917E38E4CA489A2B8B5A8FB78BFE70416D79F13D5CF275292D518B22` |
| `dsh-fairy-visual.tgz` | 382827 | `C5893569B86A53CC6BEE18D6A46EB44787F93E449A2B7F05C475565B48300A7F` |
| `dsh-fairy-voice.tgz` | 52717 | `25B79BDD09FEE56A025D67172A67365219C6680628C382018BAA61E348D9487F` |
| `dsh-balance-meter.tgz` | 12791 | `454F6DE02A825A9C5D4CAF75E214660AB727AA6FB389A35B63660697B8A72693` |
| `dsh-browser-dock.tgz` | 29592 | `03460446635A5F21DA3E4DBA11D83AF0EFA55564E288BDE1ECF2FCE5C29E4986` |
| `dsh-fairy-startup.tgz` | 5018 | `EE3C28A3231194CF5670526DA5B85F05BA3B09D4228275D02DD6ED54B8AFF452` |

**合计 471.6 KB。** Release 页（已被取代）：
https://github.com/Guzhou2002/Fairy-DSH-Optimized/releases/tag/v0.3.0 —— 附件同为上述 7 个；
当时只核对了字节数，**未逐个复算 SHA**（该验证已在 v0.3.1 补齐，见上）。

> 🔴 **`install.cmd` 已改成 GBK 编码**（见 §9 坑 14）。旧版是 UTF-8，在 **936 中文系统**上会让 cmd
> 认错行尾、注释行被当成命令、满屏 `is not recognized`，**群友几乎无一能装上**。
> 该修复是在 0.3.0 期间完成的，两个安装器字节数至今未变。

### v0.2.3 及更早（历史）

- 分发物：两个 zip（**中文名**），Release 附件用**英文名**；现已被 `.tgz` 方案取代
- 旧流程脚本归档在 `legacy\`：`install-0.2.3.ps1` / `uninstall.ps1` / `build-release.ps1` / `安装说明.txt` / `patches`
- 旧文档存档：`docs\旧版说明-v0.2.3.md`
- **校验值：随 zip 一起删除，不再维护**

## 9. 注意事项（踩过的坑，别重踩）

1. **`.ps1` 必须 UTF-8 带 BOM**：编辑工具常把 BOM 弄丢 → PowerShell 5.1 按 GBK 读 → 满屏假语法错。改完必须补 BOM + `Parser::ParseFile` 校验
2. **PowerShell 两个坑**：`\"` 不是转义（反斜杠是原样，会截断字符串）；`$url?name=` 里的 `?` 会被当成变量名的一部分，要写 `${url}?name=`
3. **绝不要在这个目录跑 `git clean -fd`**：`node_modules` 与 `release/` 都在忽略列表里，跑一下插件当场加载不了
4. **`lib\client.js` 是生成物**：合并上游时**不要直接接受上游对它的改动**，要先把新版存进 `upstream-originals\` 再重新生成（见 `docs\仓库与上游.md` §4.2）
5. **人设预设已搬进插件包**：上游更新语料时 git 会报 `deleted by us`，要手动同步进 `fairy-visual\dsh-fairy-visual\.agent-presets\fairy\`；且 `agent.cordis.yml` 里那两条私有 runtime 引用要**保持移除**
6. **GitHub Release 附件名不能含中文**（会被清洗成 `-.zip`）；正文要写中英对照表
7. **live profile 用 `link:` 指向本目录**：任何改写文件的 git 操作（checkout / merge / reset --hard）会**立刻影响正在运行的 DSH**；规矩是"先提交再切分支/合并"
8. **本机网络**：GitHub 大文件下载会卡（`git clone`、Release 附件下载），但 API 与 `ls-remote` 正常 —— 别用"下载回来验证"当默认手段
9. **别把正在跑的 DSH 当残留进程杀掉**：GUI 监听 `127.0.0.1:3080`（曾差点误杀，清理前先按端口/命令行确认归属）
10. **对外口径**：本项目是**非官方**整理分支，README / 安装说明 / Release 正文都已写明"问题请反馈到本仓库或群，不要打扰上游作者"
11. **改完文档要重打包**：`安装说明.txt` 在 zip 里，改它就等于改包 → SHA 会变 → Release 附件与正文 SHA 必须同步更新，tag 也要对齐
12. **git 的代理要单独配**：本机梯子**只给浏览器指路**，git 默认裸连，会报 `Failed to connect to github.com:443 after 21xxx ms`（有时还报 `schannel: SEC_E_NO_CREDENTIALS`）。
    **排障三步**：① 看系统代理 `HKCU:\Software\Microsoft\Windows\CurrentVersion\Internet Settings` 的 `ProxyServer`；② 看那个端口在不在监听；③ 看 `git config --get-regexp proxy` 是不是空的（空的 = 就是它）。
    **修复**：`git config --global http.https://github.com.proxy http://127.0.0.1:7897`（**换梯子端口后要同步改**）。
    **撤销**：`git config --global --unset http.https://github.com.proxy`。
    > ⚠️ 原先"本机网络 GitHub 大文件下载会卡"的结论是**误诊** —— 真正原因是 git 没走代理。配好后 `ls-remote`/`push` 都正常。
13. **`upstream-pull-request\` 是 git worktree，不是普通文件夹**：它和主仓库**共享 `.git`**。
    ① 它已被写进 `.git/info/exclude`（**本地忽略，不动 `.gitignore`**）；
    ② **绝不要在主仓库跑 `git add -A`**，否则可能把整份上游代码提交进 main；
    ③ 要删它用 `git worktree remove upstream-pull-request`，**别直接删文件夹**（会留下脏元数据）；
    ④ `git worktree list` 可以看到两个工作区。
14. **🔴 `install.cmd` / `install_full.cmd` 必须是 GBK 编码 + `chcp 936`，绝不能存成 UTF-8**（排查了一整轮的血泪教训）：
    UTF-8 的中文在 **936 代码页**的系统上会让 `cmd.exe` 认错行尾换行符，
    于是 `rem` 注释行被**从中间切开当成命令执行**，满屏
    `'xx' is not recognized as an internal or external command`，安装必然失败。
    - **症状**：双击后开头一堆乱码报错，但标题和部分 `echo` 行又显示正常，很有迷惑性
    - **为什么在开发机复现不出来**：本机 ANSI 代码页是 **65001**（Win11 开了全局 UTF-8），恰好躲过这个 bug
    - **判别方法**：`chcp` 显示 **936 = 会中招**，显示 65001 = 不会
    - **改完必须**：GBK 严格编码写回 + 全 CRLF + 无 BOM + `chcp 936`；
      且不能出现 GBK 存不下的字符（emoji 等，原 `✅` 已因此删掉）
    - **验证方法**：本地空跑（把 `call dsh plugin` 块和 `robocopy` 行换成 `echo`）应 **0 条解析错误**；
      但**本机验证不算数**，最终必须在真机（936）跑一遍
    - 原理参考：http://www.bathome.net/forum.php?mod=viewthread&tid=68945

15. **git push 报 `schannel: SEC_E_NO_CREDENTIALS` 时，先别急着"绕开代理"**（2026-09-13 实测）：
    - **先查三样**：`git config --get-regexp proxy`、系统代理注册表 `ProxyServer`、
      **梯子端口在不在监听**（`Test-NetConnection 127.0.0.1 -Port 7897`）
    - 本次实测：梯子**开着**、代理也**配着**，用 §9 坑 12 那招"绕开代理"反而**直接失败**
      （直连 GitHub 走 schannel 取不到凭据）
    - 真正原因是**进程受限**（沙箱 / 受限令牌）下 schannel 拿不到 Windows 凭据存储；
      **放宽权限后、带代理一次成功**。换 `http.sslBackend=openssl` 会去拉凭据助手脚本，
      报 `couldn't create signal pipe, Win32 error 5`（同样是被拦）—— 属于同一类限制
    - **结论**：先确认梯子在跑，再考虑权限；"绕开代理"只适用于**梯子确实不通**的情况。
      另外：**能走代理时，Release 附件下载回来复算 SHA 是可行的**（7 个文件跑得很快），
      §5 里"本机下载 GitHub 大文件会卡死"的旧结论作废

16. 🔴 **用户数据绝不能放进插件目录**（2026-09-13 踩了一轮才想明白）：
    - **现象**：看到参考音频默认在 `~/.dsh/fairy-voice/reference/`，而插件装在
      `~/.dsh/plugins/Fairy-DSH/fairy-voice/dsh-fairy-voice/`，第一反应是"路径写错了，
      文件应该在插件目录里"。于是把默认位置往顶层挪，想避开 `fairy-voice` 这个撞名。
    - **真相**：`~/.dsh/fairy-voice/` 是**插件的数据目录**，由插件**运行时**用 `homedir()` 自己建，
      跟插件装在哪无关。两个路径**故意不同、也必须不同**。
    - **为什么必须分开**：`dsh plugin add <tgz>` 会把包解进 **profile 下的 `node_modules`**，
      而那正是 **pnpm 随时会清掉 / 重建 / 按版本重装**的目录。
      把音色、API Key 放进去 = 放在定时炸弹上，**升一次版本就没了**。
      源码装法（`git clone` / `link:`）同样会被 `git pull` / 重 clone 影响。
    - **规矩**：用户数据一律 `~/.dsh/fairy-voice/`（`reference/` 音色、`runtime/config.json` 设置、
      `voice-brain.json` 密钥）。判据一句话：**任何会被 `pnpm install` / 插件重装影响到的目录，
      都不许放用户数据。**
    - **参考音频的位置沿革**（别再折腾了）：`runtime/reference/`（最初）→
      短暂试过顶层 `~/.dsh/fairy-DSH-voice-reference/`（收回）→ **现为 `~/.dsh/fairy-voice/reference/`**。
      旧位置保留兼容回退；该目录插件启动时自动创建，并写一份 `README.txt`。

## 10. 环境事实（本机）

- DSH `0.1.2-rc.1`、Node 24.20、pnpm 11.25、git 2.55、Windows
- 硬件：RTX 2060 6GB / i5-10300H / 16GB；**C 盘剩余少**（装 SoVITS 用 D:/E:）；Python 3.14（需 3.10/3.11，建议 uv）；无 ffmpeg / conda
- live profile（`C:\Users\1\.dsh\profiles\web`）：`dsh-fairy-visual`、`dsh-fairy-voice`、`dsh-balance-meter` 以 `link:` 装入；`browser-dock`、`fairy-startup` 未装
- `settings.yaml`：`fairy-visual.enabled = false`、`agent-presets.default = fairy`；`.agent-presets/fairy` 已安装
- 参考音频与语音配置**已就位**；0.3.2 起参考音频位置为 `~/.dsh/fairy-voice/reference/fairy_ref.wav`（该目录插件启动时自动创建并附 README.txt；历史位置 `runtime/reference/` 仍兼容）。**注意：用户数据一律放 `~/.dsh/fairy-voice/`，绝不能挪进插件目录** —— 理由见 §9 坑 16。本机**未跑 GPT-SoVITS**，朗读改走 MOSS-TTS-Nano（`127.0.0.1:18083`）
- 五个插件版本：`dsh-fairy-visual 0.1.0`、`dsh-fairy-voice 1.0.0`、`dsh-balance-meter 0.2.0`、`dsh-browser-dock 0.1.0`、`dsh-fairy-startup 1.0.0`

## 11. 常用命令

```powershell
cd C:\Users\1\.dsh\plugins\Fairy-DSH

# 重新生成设置栏合并包（改动 lib/settings-merge.ps1 或换上游原件后必做）
. .\lib\settings-merge.ps1; Set-SettingsMerge -Root (Get-Location) -Mode merged -Snapshot -Force
node --check .\fairy-visual\dsh-fairy-visual\lib\client.js
node --check .\fairy-voice\dsh-fairy-voice\lib\client.js

# 打包（5 个 tgz + install.cmd + SHA256SUMS.txt，输出到 release\）
# 输出文件名一律不带版本号，直接就能上传 GitHub Release
.\pack.ps1

# 隔离验证（不碰 live profile）
.\verify-isolated.ps1                  # 数字键菜单
node .\tools\settings-probe.mjs <url>  # 设置面板探针（需先起隔离实例）

# 合并上游更新
git fetch upstream
git log --oneline main..upstream/main
git merge upstream/main                # 合完必须重新生成合并包 + 重新验证

# 发版
# 1) 改仓库根 VERSION 文件（整理版版本号，会显示在设置页顶部）
#    + 改动了哪些插件包，就把那些包的 package.json 的 version 往上加
#      （install.cmd 与插件市场都靠「包版本」判断有没有更新）
#    + 更新 README / RELEASE-NOTES
# 2) 生成合并包 → .\pack.ps1 -ReleaseNames → 隔离验证
#    ⚠️ 绝不要用 git add -A（见 §9 坑 13：会把 upstream-pull-request\ 的上游代码吸进来）
git add VERSION README.md RELEASE-NOTES.md "docs/交接摘要.md" lib/settings-merge.ps1 `
        fairy-visual/dsh-fairy-visual/lib/client.js fairy-visual/dsh-fairy-visual/package.json `
        fairy-voice/dsh-fairy-voice/lib/client.js fairy-voice/dsh-fairy-voice/package.json
git commit -m "release: v0.x …"; git tag v0.x; git push origin main --tags
#    若 push 报 schannel SEC_E_NO_CREDENTIALS → 先看 §9 坑 15（别急着绕代理）
# 3) GitHub Releases 新建 release，附件用【不带版本号】的那 5 个 .tgz
#    （README 里用的是 .../latest/download/<包名>.tgz 这种永久地址，带版本号下次发版会 404）

# 已被弃用的旧脚本已全部挪进 legacy\，说明见 legacy\README.md
# （install-0.2.3.ps1 / uninstall / build-release / 安装说明 / patches）
```

## 12. 版本索引（按版本）

> 每个版本的**产物与 SHA256** 见 §8 对应小节；**详细变更说明**见 `RELEASE-NOTES.md`。
> 本节只做索引，**不替代**上面的内容。

| 版本 | 日期 | tag / 发布页 | 核心内容 | 产物与 SHA | 详细说明 |
| --- | --- | --- | --- | --- | --- |
| **0.3.2** | 2026-09-13 | `v0.3.2` | **新增第二个朗读引擎 MOSS-TTS-Nano（CPU 就能跑）** + MOSS 一键安装脚本与自带服务端 + 引擎优缺点对照卡 + 设置面板重排（先配置后验证）与自检折叠 + 修「重新自检会重置表单」「自检不跟引擎走」 | §8 → v0.3.2 | `RELEASE-NOTES.md` v0.3.2 |
| **0.3.1** | 2026-09-13 | `v0.3.1` | **修「消息识别」读不到消息**（适配 DSH `0.1.2-rc.1` 的 `useChat`）+ `runningCalls` 兜底 + 设置页底部「诊断信息」栏 + 右下角语音异常提示 | §8 → v0.3.1 | `RELEASE-NOTES.md` v0.3.1 |
| **0.3.0** | 2026-09-13 | `v0.3.0` | 分发方式 zip → **7 个文件（5 tgz + 两个 `.cmd`）**；删 installer；`install.cmd` 改 **GBK** 修乱码；脚本拆成"只装 3 个 / 全 5 个" | §8 → v0.3.0 | `RELEASE-NOTES.md` v0.3.0 |
| **0.2.3** | 2026-09-12 | `v0.2.3` | zip 分发；**朗读自检面板**（7 项）；SoVITS 地址 / 参考音频可配置；人设一键设为默认预设；首次使用红框提示 | zip 已删（见 `legacy\`） | `docs\旧版说明-v0.2.3.md` |
| **0.2.1 / 0.2.0** | — | — | `dsh-fairy-contracts` 源码**内联进 `vendor/`**、依赖改 **hoisted 扁平**、各包自带 `dsh.bundle.patch` → 可独立分发 | — | §4.2 / §4.3 |
| 上游原始版 | — | `d639887` | 橙汁本色的 Fairy-DSH 原始代码（Apache-2.0） | — | `docs\仓库与上游.md` |

### 版本号约定

- **整理版版本号**（仓库根 `VERSION`）：决定设置页顶部显示的那行
- **各插件包版本号**（各包 `package.json` 的 `version`）：**决定 `dsh plugin add` 与插件市场能不能认出更新** ——
  **改了哪个包就必须把它 +1**，否则已装过的群友检测不到
- 两者**必须同步发版**：本次 0.3.1 就是 `VERSION` 0.3.0→0.3.1，同时
  `dsh-fairy-visual` 0.1.1→0.1.2、`dsh-fairy-voice` 1.0.0→1.0.1
